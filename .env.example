# Environment Variables Example
# Copy this to .env and .dev.vars (for Wrangler) and fill in your actual values

# Cloudflare D1 Database (configured in wrangler.jsonc)
# These are handled by Cloudflare Workers runtime

# Secrets (managed through Cloudflare secrets store)
# Add these to your Cloudflare secrets store:
# WEDDING_ADMIN_PASSWORD=your-secure-admin-password
# WEDDING_RESEND_API_KEY=re_your-resend-api-key

# Development only (for .dev.vars file)
# ADMIN_PASSWORD=your-development-admin-password
# RESEND_WEDDING_API_KEY=re_your-development-resend-api-key